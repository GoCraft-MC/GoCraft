# Git workflow

- Attribute all new commits to `MaathDEV23`, using the email `174096315+MaathDEV23@users.noreply.github.com`.
- Authenticate GitHub pushes as `MaathDEV23`. Verify the authenticated account before committing or pushing; do not use `el211` or another account.
- Keep each commit at or below 100 changed lines, counting additions and deletions together. Split larger changes into coherent commits and check the staged diff before each commit.
- Preserve unrelated user changes when staging.
- Write commit messages in English.
